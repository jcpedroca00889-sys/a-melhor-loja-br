# SATOSHII STORE — variáveis de ambiente (NUNCA commitar tokens reais)
# Pagamentos
PAYMENTS_MODE=simulation
MP_ACCESS_TOKEN=your-mercadopago-access-token-here
MP_WEBHOOK_SECRET=your-webhook-secret-here
# URL pública usada como notification_url do webhook (default: http://localhost:PORT)
# MP_NOTIFICATION_URL=https://sua-loja.com/api/webhooks/mp

# Backend URL (para o frontend apontar em produção)
# VITE_API_URL=https://seu-backend.vercel.app

# CORS (produção)
# ALLOWED_ORIGINS=https://sua-loja.com,https://admin.sua-loja.com